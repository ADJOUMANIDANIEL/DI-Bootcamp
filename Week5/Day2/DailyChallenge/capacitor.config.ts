import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'projest_Daily',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;

import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Exercice2Ninja',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
